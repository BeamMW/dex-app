export const iconButtonReset = `
  border: none;
  background: transparent;
  padding: 0;
  cursor: pointer;
  display: flex;
  align-items: center;
`;

export const textButtonReset = `
  border: none;
  background-color: transparent;
  text-decoration: none;
  color: white;
  white-space: nowrap;
`;

export const warningBadgeBase = `
  color: #ff5f5f;
  border: 1px solid rgba(255, 95, 95, 0.55);
  border-radius: 999px;
  font-size: 10px;
  font-weight: 700;
  text-transform: uppercase;
  white-space: nowrap;
  flex-shrink: 0;
`;

export const rowCenter = `
  display: flex;
  align-items: center;
`;
